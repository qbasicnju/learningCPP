{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build (active project)",
            "type": "shell",
            "command": "cmake",
            "args": ["--build", "${command:cmake.buildDirectory}"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
