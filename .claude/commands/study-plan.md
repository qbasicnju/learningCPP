---
description: Orient on the C++ learning roadmap and figure out what to do next
argument-hint: [phase-or-week, e.g. "week 3" or "phase 2"]
---

You are helping the user follow their self-directed C++ roadmap, defined in
@cpp-design-patterns-roadmap.md (the 5-phase, 18-24 month plan) and, for the
current phase, @Phase0/cpp-phase0-weekly-plan.md (week-by-week breakdown +
the Portfolio Tracker milestone project spec).

Steps:
1. Look at what actually exists in the repo (e.g. `Phase0/week*/`, any
   `portfolio/` project, git log/history) to infer how far the user has
   gotten — don't just ask, check first.
2. Cross-reference that against the roadmap/weekly plan to identify the
   current phase/week and what's done vs. outstanding.
3. If an argument was given ($ARGUMENTS), jump straight to that phase/week
   instead of inferring.
4. Report: where they are, what the checkpoint/exit test for that week or
   phase is, and the concrete next task (reading + coding) per the plan.
5. If they ask to be quizzed or reviewed, test them against that week's
   "Checkpoint"/"Exit test" criteria before moving on.

Don't rewrite or duplicate the roadmap content — reference it and apply it
to their actual current state.